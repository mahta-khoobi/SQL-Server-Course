--select ChildName,ParentName
--	from Child inner join Parent
--		on parent.parentId=child.parentref


--select parentname,childname
--	from parent left join child
--	on parent.parentid=child.parentref

--select parentname,childname
--	from parent right join child
--	on parent.parentid=child.parentref

--select parentname,childname
--	from parent full join child
--	on parent.parentid=child.parentref

--select parentname,parentcode,childcode,childname
	--from parent cross join child
	